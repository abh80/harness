{
  "name": "create-snap-harness",
  "version": "0.1.0",
  "author" : "abh80",
  "type": "module",
  "bin": { "create-snap-harness": "./dist/cli.js" },
  "scripts": {
    "build": "tsc -p .",
    "test": "vitest run",
    "dev": "tsx src/cli.ts",
    "prepublishOnly": "npm run build && npm test"
  },
  "files": ["dist", "templates"],
  "engines": { "node": ">=20" },
  "dependencies": {
    "@inquirer/prompts": "^7.0.0",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
